{
  "name": "nature-remo",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "preinstall": "echo \"preinstall command executed\" && exit 0",
    "postinstall": "cd lambda && npm i && cd ../ && exit 0",
    "beforedeploy": "echo \"beforedeploy executed\" && exit 0",
    "finalizedeploy": "echo \"finalizedeploy executed\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "crypto": "^1.0.1",
    "serverless-plugin-scripts": "^1.0.2",
    "sample": "git+ssh://git@github.com:smt7174/sample.git"
  },
  "devDependencies": {
    "crypto-js": "^4.0.0",
    "moment": "^2.26.0"
  }
}
